import "@shopify/ui-extensions/preact";
import { render } from "preact";
import { MarginLabBlock } from "./MarginLabBlock";

export default function () {
  render(<MarginLabBlock />, document.body);
}
