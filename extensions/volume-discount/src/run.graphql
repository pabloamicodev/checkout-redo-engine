query RunInput {
  cart {
    lines {
      id
      quantity
      cost {
        totalAmount {
          amount
        }
      }
      merchandise {
        ... on ProductVariant {
          product {
            id
            metafield(namespace: "custom", key: "volume_discount_tiers") {
              jsonValue
            }
          }
        }
      }
    }
  }
}
